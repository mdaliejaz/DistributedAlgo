import sys
import da
import uuid

class Coordinator(process):
	def setup():
		pass

	def evaluatePolicy():
		evaluationID = getUniqueID()
		timestamp = logical_clock()

	def getUniqueID():
		return uuid.uuid4()

	def receive(msg= ('client',)):
		output('client hello')
		
	def receive(msg= ('worker',subjectID, resourceID)):
		output('worker hello - ' + str(subjectID))

	def run():
		output('starting...')
		await(received(('done',)))